import thunk from 'redux-thunk';
import { module, test } from 'qunit';

module('Unit | Utility | require thunk');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.ok(thunk);
  assert.equal(typeof thunk, 'function');
});
